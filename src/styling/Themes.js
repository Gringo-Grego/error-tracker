import { COLORS } from "../utils/constants";


export const THEMES = {
    light: {
        '--color-danger': COLORS.RED,
        '--color-primary-dark': COLORS.BLUE,
        '--color-primary-light': COLORS.LIGHT_BLUE,
        '--color-white': COLORS.WHITE,
        '--color-secondary-light': COLORS.YELLOW,
        '--color-secondary-dark': COLORS.DARK_YELLOW,
        '--color-warning': COLORS.YELLOW,
        '--color-background': COLORS.WHITE,
        '--color-font': COLORS.BLACK,
        '--color-shadow':COLORS.DARK_SHADOW,
        '--color-accent':COLORS.CHARCOAL,

    },
    dark: {
        '--color-danger': COLORS.PINK,
        '--color-primary-dark': COLORS.BLUE,
        '--color-primary-light': COLORS.LIGHT_BLUE,
        '--color-white': COLORS.WHITE,
        '--color-secondary-light': COLORS.YELLOW,
        '--color-secondary-dark': COLORS.DARK_YELLOW,
        '--color-warning': COLORS.YELLOW,
        '--color-background': COLORS.CHARCOAL,
        '--color-font': COLORS.WHITE,
        '--color-shadow':COLORS.WHITE,
        '--color-accent':COLORS.CHARCOAL,
    }

}